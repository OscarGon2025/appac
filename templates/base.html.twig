<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{% block title %}Site des Plaisanciers{% endblock %}</title>

    {# CSS du thème #}
    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ asset('css/bootstrap-icons.css') }}" rel="stylesheet">
    <link href="{{ asset('css/templatemo-festava-live.css') }}" rel="stylesheet">


    {# overrides #}

    <link href="{{ asset('css/custom.css') }}" rel="stylesheet">

    {# Polices #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@100;200;400;700&display=swap" rel="stylesheet">

    {# (Optionnel) styles additionnels spécifiques aux pages #}
    {% block stylesheets %}{% endblock %}


    <style>html{scroll-behavior:smooth}</style>

    {# Google reCAPTCHA v3 #}
    {% if recaptcha_site is defined %}
        <script src="https://www.google.com/recaptcha/api.js?render={{ recaptcha_site }}"></script>
    {% endif %}



{# Classe optionnelle sur <body> (ex: 'page-login')#}
{% set body_class = body_class|default('') %}
{# Contrôle du mode de la navbar : true = transparente (Home), false = solide (pages filles)#}
{% set navbar_is_transparent = navbar_is_transparent|default(false) %}

<body class="{{ body_class }}">

<header id="site-top">
    {{ render(path('site_header')) }}               {# bandeau meteo/marées #}
</header>

{# Navbar via partial #}
{% include 'partials/_navbar.html.twig' with {  # navbar
navbar_is_transparent: navbar_is_transparent|default(false)
} %}


<main>
    {% block body %}{% endblock %}
</main>

{% include 'partials/_footer.html.twig' %}

{# JS (ordre recommandé par le thème) #}
<script src="{{ asset('js/jquery.min.js') }}"></script>
<script src="{{ asset('js/bootstrap.min.js') }}"></script>
<script src="{{ asset('js/jquery.sticky.js') }}"></script>
<script src="{{ asset('js/click-scroll.js') }}"></script>
<script src="{{ asset('js/custom.js') }}"></script>


{% block javascripts %}{% endblock %}

{# reCAPTCHA v3 injection pour tous les formulaires #}
{% if recaptcha_site is defined %}
    <script>
        grecaptcha.ready(function() {
            document.querySelectorAll('form').forEach(function(form) {
                form.addEventListener('submit', function(e) {
                    grecaptcha.execute('{{ recaptcha_site }}', {action: 'register'}).then(function(token) {
                        let input = document.createElement('input');
                        input.type = 'hidden';
                        input.name = 'g-recaptcha-response';
                        input.value = token;
                        form.appendChild(input);
                        form.submit();
                    });
                    e.preventDefault();
                });
            });
        });
    </script>
{% endif %}

</body>
</html>

